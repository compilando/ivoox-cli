#!/usr/bin/env python3
"""Entrada de desarrollo; la instalación crea el mismo comando con pyproject.toml."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from ivoox_cli.cli import entrypoint

sys.exit(entrypoint())
